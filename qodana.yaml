# qodana.yaml
version: "1.0"
linter: jetbrains/qodana-jvm:2024.2
projectJDK: "21"
profile:
  name: qodana.recommended
exclude:
  - name: All
    paths:
      - target
      - .idea
failThreshold: 100000   # tillad mange findings uden at fejle CI
